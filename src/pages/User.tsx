import React from "react";
import { Menu, SideBarMenu } from "../Components";

function User() {
    return (
        <div className="User">
            <Menu />
            <SideBarMenu />
            user
        </div>
    );
}

export default User;
